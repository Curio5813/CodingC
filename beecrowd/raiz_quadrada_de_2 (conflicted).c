#include <stdio.h>


int main(){

    int n;
    double raiz, denominador = ;

    scanf("%d", &n);
    for(int i = 0; i < n; i++){
        raiz = 1 + (1 / (2 + (1 / 2)));
        raiz_quadrada /= raiz;

    }
    printf("%.10lf\n", raiz);


    return 0;
}